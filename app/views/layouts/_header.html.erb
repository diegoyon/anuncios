<header class="navbar navbar-expand-sm navbar-dark bg-primary">
  <div class="container-fluid">
    <%= link_to 'Proyecto 2', announcements_path, class: "navbar-brand" %>
    <% if user_signed_in? %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <nav class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item"><%= link_to "New Announcement", new_announcement_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "User", edit_user_registration_path, class: "nav-link" %></li>
          <li class="nav-item"><%= button_to "Logout", destroy_user_session_path, method: :delete, class: 'btn btn-danger nav-link' %></li>
        </ul>
      </nav>
    <% end %>
  </div>
</header>
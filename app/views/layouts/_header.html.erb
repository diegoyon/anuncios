<header class="navbar navbar-expand-sm navbar-dark bg-primary">
  <div class="container">
    <% if user_signed_in? %>
      <%= link_to 'Proyecto 2', announcements_path, class: "navbar-brand" %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <nav class="collapse navbar-collapse flex-grow-0" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item"><%= link_to "New Announcement", new_announcement_path, class: "nav-link text-white" %></li>
          <li class="nav-item"><%= link_to "Profile", user_show_path, class: "nav-link text-white" %></li>
          <li class="nav-item"><%= button_to "Logout", destroy_user_session_path, method: :delete, class: 'btn btn-outline-light' %></li>
        </ul>
      </nav>
    <% else %>
      <%= link_to 'Proyecto 2', root_path, class: "navbar-brand" %>
    <% end %>
  </div>
</header>